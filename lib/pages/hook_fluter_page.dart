// HookWidget
// flutter_hooks

import 'package:flutter/material.dart';
import 'package:flutter_hooks/flutter_hooks.dart';


class HookFlutterPage extends HookWidget {
  const HookFlutterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('HookFlutterPage'),
      ),
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}